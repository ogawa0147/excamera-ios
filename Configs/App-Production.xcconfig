#include "Pods/Target Support Files/Pods-Camera/Pods-Camera.release.xcconfig"
#include "App-Base.xcconfig"

SWIFT_COMPILATION_MODE = wholemodule
ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon
CODE_SIGN_IDENTITY = Apple Distribution
DISPLAY_NAME = Camera
PRODUCT_BUNDLE_IDENTIFIER = jp.excamera
PROVISIONING_PROFILE_SPECIFIER = match AppStore jp.excamera
CODE_SIGN_ENTITLEMENTS = Camera/Entitlements/Production.entitlements
